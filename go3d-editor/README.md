# go3d-editor — 3D 建模编辑器（SolidWorks 风格界面 + Blender 风格视口）

基于 go3d 软渲染引擎的桌面 3D 建模编辑器，纯 Go 零 cgo。
界面参照 SolidWorks：顶部工具栏 / 左侧模型树 / 中间 3D 视口 / 右侧属性面板 / 底部状态栏。
建模逻辑参照 SolidWorks 特征树 + Blender 视口操作（轨道相机、Gizmo 变换、正交/透视）。

## 功能（两大模块）

### 模块一：建模
- 6 种基本体：立方体/球体/圆柱/圆锥/圆环/平面
- **草图绘制**（SolidWorks 核心工作流）：折线/矩形/圆工具、前视/顶视/右视基准面、
  视口点击绘制、Enter 闭合、**拉伸成实体**
- **布尔运算**：并集/差集/交集（CSG：互裁剪 + 补面 + 包围盒剔除，立方体-球体 11ms/588 面）
- 变换：Gizmo 拖拽（移动/旋转/缩放）+ 属性面板数值输入
- 特征树：添加/删除/复制/重命名/可见性
- **OBJ 导入/导出**（主流建模软件互通）
- **STL 导入**（二进制/ASCII 自动识别，3D 打印/网格交换通用格式）
- **GLTF/GLB 导入 + GLB 导出**（glTF 2.0：JSON accessor/bufferView/buffer 解析，
  内联 base64/外部 buffer/GLB 二进制 chunk 全支持，与 Blender 直接互通）
  （工具栏第一行 OBJ↓/OBJ↑/STL↓/GLB↓/GLB↑ 按钮，建模模式显示）

### 模块二：骨骼绑定 + 白模动画
- **骨骼系统**：层级树（根/子骨骼）、添加/删除/重选、Gizmo 编辑局部变换、绿色骨线可视化
- **顶点绑定**：自动权重（最近 4 骨骼距离倒数加权）、绑定姿势管理
- **白模动画**：关键帧（每骨骼 位置/旋转）、时间轴（左右方向键推进）、播放/暂停/停止/循环、
  骨骼动画驱动网格蒙皮变形

## 启动

```bash
bash build.sh
./bin/go3d-editor                # 980x620 窗口
./bin/go3d-editor --res 1280x720
./bin/go3d-editor --load scene.json   # 启动加载场景
```

## 界面

```
┌──────────────────────────────────────────────┐
│ 工具栏：＋立方体 ＋球体 ＋圆柱 ＋圆锥 ＋圆环 ＋平面 │ 保存 载入 │
│ 移动[G] 旋转[R] 缩放[S] │ 顶视 前视 右视 轴测 │ 透视 P │ 网格 │
├──────────┬──────────────────────────┬──────────┤
│ 模型树    │                          │ 属性      │
│ ▸ 零件1   │       3D 视口             │ 名称/类型 │
│  □Cube1  │   (轨道相机 + 网格 + 轴)   │ 位置 XYZ  │
│  □Sphere2│                          │ 旋转 XYZ  │
│          │                          │ 缩放/颜色 │
│          │                          │ 复制/删除 │
├──────────┴──────────────────────────┴──────────┤
│ 状态栏：消息 / 特征数 / FPS / 相机目标          │
└────────────────────────────────────────────────┘
```

## 操作

| 操作 | 说明 |
|---|---|
| Tab | 切换编辑模式（建模→草图→骨骼→动画） |
| 左键点击 | 视口拾取选中 / 树节点选中 / 属性字段聚焦 |
| 左键拖拽 Gizmo | 移动/旋转/缩放选中物体或骨骼（红X 绿Y 蓝Z） |
| 中键拖拽 | 轨道旋转视图 |
| 右键拖拽 | 平移视图（Pan） |
| 滚轮 | 缩放视图 |
| G / R / S | 切换 移动/旋转/缩放 模式（工具栏按钮同） |
| 1 / 2 / 3 / 7 | 前视 / 等轴测 / 右视 / 顶视 |
| P | 透视/正交切换 |
| F | 选中物体取景 |
| Del | 删除选中特征 |
| C | 复制选中特征 |
| H | 显示/隐藏 |
| 空格 | 网格开关 |
| 双击树节点 | 重命名 |
| Ctrl+S / Ctrl+O | 保存 / 载入（scene.json） |
| F1 | 帮助 |
| 草图模式 | 折线/矩形/圆 工具 → 视口点击 → Enter 闭合 → 拉伸 |
| 骨骼模式 | A 加骨骼、点击选骨骼、Gizmo 编辑、绑定权重 |
| 动画模式 | ＋关键帧、左右方向键推进时间、播放/暂停/停止 |

## 架构

```
go3d-editor/
├── ui/font.go      CJK 中文渲染（NotoSansCJK + opentype，系统字体）
├── app/
│   ├── doc.go      文档模型：特征树 + 自定义网格 + 骨骼/动画字段
│   ├── editor.go   主状态：布局/输入分发/命令/文本编辑/编辑模式
│   ├── viewport.go 轨道相机 + 视口渲染 + 拾取 + 草图/骨骼叠加
│   ├── gizmo.go    变换 Gizmo（物体/骨骼通用）+ 拖拽数学
│   ├── sketch.go   草图：2D 数据结构/耳切法三角化/拉伸体
│   ├── csg.go      布尔运算：互裁剪 + 补面 + 包围盒剔除 + 射线分类
│   ├── skeleton.go 骨骼层级/顶点权重/蒙皮/动画采样
│   ├── editmodes.go 草图/骨骼/动画模式命令 + 布尔/OBJ 命令
│   ├── panels.go   UI 面板：工具栏（模式切换）/模型树/骨骼树/属性/时间轴/状态栏
│   ├── io.go       JSON 保存/加载（含骨骼/动画/自定义网格）
│   └── io_obj.go   OBJ 导入/导出
└── cmd/go3d-editor/  入口（engine 窗口 + 字体加载）
```

## 文件格式

- 场景：JSON（含自定义网格、骨骼层级、动画轨道；权重加载时按绑定姿势重算）
- 模型：OBJ 导入/导出（与 Blender/主流建模软件互通）

## 验证记录（2026-08-09 v2）

- go test 15 项：草图三角化（凸/凹）、拉伸体、CSG 差集/并集（射线命中验证）、骨骼权重/
  蒙皮变形、动画采样、OBJ 往返、骨骼+动画 JSON 往返、草图拉伸命令、CSG 命令 全过
- X11 实测：草图矩形→闭合→拉伸出实体；骨骼添加（2 根）→绿色骨线可视化→绑定权重；
  动画录制（t0/t0.5 两帧）→播放→蒙皮变形（帧间差异 224）；布尔差集（立方体-球体 588 面 11ms）
- CSG 性能：包围盒剔除后 59292 面 → 588 面（球体 576 面场景）
```

## 协议

MIT License（见 LICENSE）

## 开发环境

- 设备：小米手机（MIUI / Android 13）
- 环境：Termux（Android 终端）+ termux-x11 + XFCE 图形桌面
- 语言：Go / Python 为主，纯 CLI 开发
- 注意：本项目在 Android / Termux 上开发与测试，其他平台运行可能需要调整

## 生成声明

本项目全部代码与文档由 AI 生成（Hermes Agent + DeepSeek 模型），不含一丝人类手写代码。仅供学习交流。

## 寻求帮助

本项目是 AI 生成的实验性游戏/引擎，仍需社区帮助测试与改进：
- 欢迎提交 Issue 反馈 Bug、卡关、体验问题
- 欢迎 PR 改进玩法、数值、画面、平台兼容性
- 目前主要在 Android / Termux 上测试，欢迎在其他平台测试反馈
